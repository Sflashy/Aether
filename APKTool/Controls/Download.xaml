<UserControl
    x:Class="APKTool.Controls.Download"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:APKTool.Controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid
        Background="{DynamicResource CustomDarkerBackgroundColor}"
        Opacity="0.8"
        Visibility="Visible">
        <materialDesign:Card
            Width="400"
            Height="190"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{DynamicResource CustomDarkBackgroundColor}"
            UniformCornerRadius="10">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <TextBlock
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        FontSize="18"
                        Text="Downloading Frida Gadget" />
                    <Button
                        Padding="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Style="{StaticResource MaterialDesignToolForegroundButton}">
                        <Button.Content>
                            <materialDesign:PackIcon
                                Width="20"
                                Height="20"
                                Margin="0,0,0,15"
                                Kind="Close" />
                        </Button.Content>
                    </Button>
                </Grid>
                <Grid Grid.Row="1" Margin="0,15,0,0">
                    <StackPanel>
                        <Grid>
                            <TextBlock
                                FontWeight="Medium"
                                Foreground="{StaticResource CustomPrimaryTextColor}"
                                Text="{Binding FridaAsset.Name}" />
                            <TextBlock HorizontalAlignment="Right" FontWeight="Medium">
                                <Run Text="{Binding DownloadProgress}" />
                                <Run Text="%" />
                            </TextBlock>
                        </Grid>
                        <ProgressBar
                            Height="6"
                            Margin="0,5,0,0"
                            Foreground="{DynamicResource CustomNeonColor}"
                            IsIndeterminate="False"
                            Maximum="100"
                            Minimum="0"
                            Style="{StaticResource CleanProgressBar}"
                            Value="30" />
                        <Grid Margin="0,5,0,0">
                            <TextBlock
                                FontSize="13"
                                FontWeight="Medium"
                                Foreground="{DynamicResource CustomSecondaryTextColor}"
                                Text="7.2 MB/s" />
                            <TextBlock
                                HorizontalAlignment="Right"
                                FontSize="13"
                                FontWeight="Medium"
                                Foreground="{DynamicResource CustomSecondaryTextColor}"
                                Text="12.7 MB / 15.7 MB" />
                        </Grid>
                    </StackPanel>
                </Grid>
                <Grid
                    Grid.Row="2"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
                    <Button
                        Background="{DynamicResource CustomButtonBackgroundColor}"
                        BorderThickness="0"
                        Command="{Binding CancelFridaDownloadCommand}"
                        Content="Cancel"
                        Foreground="{StaticResource CustomPrimaryTextColor}" />

                </Grid>
            </Grid>

        </materialDesign:Card>
    </Grid>
</UserControl>
