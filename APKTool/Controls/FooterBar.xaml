<UserControl
    x:Class="APKTool.Controls.FooterBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:APKTool.Controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:APKTool.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <materialDesign:Card
        Padding="10"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Bottom"
        materialDesign:ElevationAssist.Elevation="Dp5"
        Background="{DynamicResource CustomDarkBackgroundColor}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Ellipse
                    Width="10"
                    Height="10"
                    Margin="0,0,5,0"
                    Fill="{Binding SelectedDevice.Status, Converter={StaticResource DeviceStatusColorConverter}}" />
                <TextBlock
                    FontSize="13"
                    FontWeight="Medium"
                    Foreground="{DynamicResource CustomSecondaryTextColor}">
                    <Run Text="{Binding SelectedDevice.Model}" />
                </TextBlock>
            </StackPanel>
            <TextBlock
                Grid.Column="1"
                HorizontalAlignment="Center"
                FontSize="13"
                FontWeight="Medium"
                Foreground="{DynamicResource CustomSecondaryTextColor}">
                <Run Text="Frida Server:" />
                <Run Text="v16.7.18" />
                <Run Text="|" />
                <Run Text="ADB: v1.0.41" />
                <Run Text="|" />
                <Run Text="Frida APK Injector: v1.0" />
            </TextBlock>
            <TextBlock
                Grid.Column="2"
                HorizontalAlignment="Right"
                FontSize="13"
                FontWeight="Medium"
                Foreground="{DynamicResource CustomSecondaryTextColor}">
                <Run Text="Memory:" />
                <Run Text="{Binding SelectedDevice.RamUsage}" />
                <Run Text="|" />
                <Run Text="CPU:" />
                <Run Text="{Binding SelectedDevice.CpuUsage}" />
            </TextBlock>
        </Grid>
    </materialDesign:Card>
</UserControl>
